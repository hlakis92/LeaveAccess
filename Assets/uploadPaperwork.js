function uploadNewPaperwork() {
    var newDoc = document.createElement("INPUT") && document.getElementById("attachPw").appendChild("newDoc");
    newDoc.setAttribute("type", "file");
    document.body.appendChild(newDoc);
}

